<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Warnock and Polivka Inc - Subscription Billing" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Stripe Membership Saas" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <div class="v-wrapper">
      <div class="v-container spaced">
        <div class="mini-container">
          <%= link_to 'Back to Victory Framework', 'http://victoryframework.com', :class => 'victory-framework-link' %>
        </div>
        <%= render 'layouts/messages_victory' %>
        <%= yield %>
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
  </body>
</html>